export class CarouselConfigModel {
  grid?: { xs: number, sm: number, md: number, lg: number, all: number };
  load?: number;
  loop?: boolean;
  touch?: boolean;
  velocity?: number;
  displayPoints?: [];
  displayArrows?: []
}
